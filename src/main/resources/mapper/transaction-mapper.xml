<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.walletservice.repository.TransactionRepository">

    <insert id="insertTransaction" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO transaction (
            account_id, amount, currency, direction,
            description, balance_after
        )
        VALUES (
                   #{accountId}, #{amount}, #{currency}, #{direction},
                   #{description}, #{balanceAfter}
               )
    </insert>

    <select id="getTransactionsByAccountId" resultType="com.walletservice.domain.Transaction">
        SELECT * FROM transaction
        WHERE account_id = #{accountId}
        ORDER BY created_at DESC
    </select>

</mapper>